# Development Guidelines

## Git & Repository Standards
- Maintain clean and descriptive commit messages.
- Always ensure the project builds successfully (`npm run build`) before pushing code.
- Preserve smart contract ABI consistency between `backend/contracts/DrugTracker.sol` and the frontend Web3 integration.
